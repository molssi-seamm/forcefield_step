name: test
channels:
  - rdkit
dependencies:
    # Base depends
  - python
  - pip
  - configargparse

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - seamm>=0.6
    - seamm_ff_util>=0.6
    - seamm-widgets>=0.6

      # Testing
    - yapf

