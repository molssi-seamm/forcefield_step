name: test
channels:
  - rdkit
  - default
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - rdkit
  - openkim-models
  - kim-query
  
    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - codecov
    - seamm>=0.9
    - seamm_ff_util>=0.9
    - seamm-widgets>=0.9

      # Testing
    - yapf

