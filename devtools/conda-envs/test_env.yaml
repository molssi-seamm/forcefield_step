name: test
channels:
  - rdkit
  - default
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - rdkit
  
    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - codecov
    - seamm>=0.9
    - seamm_ff_util>=0.9
    - seamm-widgets>=0.9

      # Testing
    - yapf

